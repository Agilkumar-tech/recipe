import "./Category.css"
import Capture from "../../../src/assert/Capture.PNG"
function Category({handlechange}) {
  return (
    <div>
      <img src={Capture} className="image"/>
    </div>
  )
}

export default Category
